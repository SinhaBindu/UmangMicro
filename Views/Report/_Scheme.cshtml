@using System.Data
@{
    ViewBag.Title = "User List";
    DataTable dt = new DataTable();
    if (Model != null)
    {
        dt = Model;
    }
}
<strong>Schemes</strong>
<div class="row row-deck">
    <div class="col-lg-12 col-md-12">
        <div class="card">
            <div class="table-responsive">
                <table class="table card-table table-vcenter text-nowrap" id="DataList1">
                    <thead>
                        <tr>
                            <th>SrNo.</th>
                            <th>Name of Scheme</th>
                            <th>Objective</th>
                            <th>Eligibility</th>
                            <th>Benefit</th>
                            <th>Remarks</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            int sno = 1;
                            if (Model != null)
                            {
                                foreach (DataRow dr in dt.Rows)
                                {
                                    <tr>
                                        <td class="text-center">@(sno++)</td>
                                        <td class="text-left">@dr["NameofScheme"]</td>
                                        <td class="text-left">@dr["Objective"]</td>
                                        <td class="text-left">@dr["Eligibility"]</td>
                                        <td class="text-left">@dr["Benefit"]</td>
                                        <td class="text-left">@dr["Remarks"]</td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr><td colspan="7" class="text-danger"><strong> Record Not Found !!</strong></td></tr>
                            }
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
